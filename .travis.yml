language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-composer-php-library' ]; then cd .. && eval "mv $currentfolder generator-composer-php-library" && cd generator-composer-php-library; fi
